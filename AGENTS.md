# Agent notes

Build, format, and test with `make build`, `make fmt`, `make test`, `make check` (fmt check + tests). See the [Makefile](Makefile) for all targets, including `make bench`.

For design, persistence, hooks, and recovery behavior, see [docs/plan/spec.md](docs/plan/spec.md).
